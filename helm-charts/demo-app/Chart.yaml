apiVersion: v2
name: demo-app
description: Demo service for the eks-platform reference environment
type: application

# appVersion is overridden by image.tag from CI; version tracks the templates.
version: 0.1.0
appVersion: "0.1.0"