apiVersion: v1
kind: ServiceAccount
metadata:
  name: demo-app
  namespace: demo
  labels:
    app.kubernetes.io/name: demo-app
# No IRSA annotation: this workload makes no AWS API calls. The database
# credential reaches it as a Kubernetes Secret, which External Secrets syncs
# out of Secrets Manager using its own identity.
automountServiceAccountToken: false