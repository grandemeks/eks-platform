# Keep the build context minimal
.git
.gitignore
*.md
Dockerfile
.dockerignore
bin/
dist/
*.test

# The compiled binary, if someone has run `go build` in here. The image is
# built from source inside the Dockerfile; shipping a host-built arm64
# executable into the context is pure upload cost at best.
app
